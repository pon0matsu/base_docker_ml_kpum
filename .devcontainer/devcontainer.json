{
    "name": "Python 3",
    "build": {
        "dockerfile": "Dockerfile",
        // コンテナ内で使用するポートを設定することもできます（例：Jupyter Notebook）
        "forwardPorts": [8888]
    },
    "settings": {
        // VS Codeの設定を定義する
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "ms-azuretools.vscode-docker",
        "mechatroner.rainbow-csv"
    ]
}
